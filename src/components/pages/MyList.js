import NavBar from "../Header/NavBar";

const MyList = () => {
    return (
        <div id="My_List">
            <NavBar />
            <h1>There is not movie yet!</h1>
        </div>
    );
};

export default MyList;

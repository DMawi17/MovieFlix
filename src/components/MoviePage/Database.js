import { useMovie } from "../../context/movie-hooks";

const Database = () => {
    const { find } = useMovie();

    return (
        <div>
            <div></div>
        </div>
    );
};

export default Database;
